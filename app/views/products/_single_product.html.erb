
<div class="product">
	<h2>name: <%= link_to a_product.name, product_path(a_product) %></h2>
	<h3>description: <%= a_product.description %></h3>
	<h4>date added: <%= a_product.created_at %></h4>
	<br>
	<strong>
		price in cents: $<%= a_product.price_in_cents %>
	</strong>
</div>